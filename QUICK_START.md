# ⚡ Быстрый старт - VPS Manager v2.0

> **Время на деплой:** ~5 минут  
> **Сложность:** Средняя  
> **Требуется:** Python 3.7+, Flask, SQLAlchemy

---

## 🚀 За 5 минут

### 1️⃣ Резервная копия (30 сек)
```bash
cp vps_manager.db vps_manager.db.backup
```

### 2️⃣ Обновление кода (1 мин)
```bash
# Скопируйте эти файлы:
# - ssh_manager.py (полностью)
# - app.py (обновленный)
# - templates/key-deployments.html (обновленный)
```

### 3️⃣ Миграция БД (1 мин)
```bash
flask db migrate -m "Add indexes to KeyDeployment"
flask db upgrade
```

### 4️⃣ Перезагрузка (30 сек)
```bash
# Если используется gunicorn:
systemctl restart gunicorn

# Если используется Flask dev сервер:
# Просто перезапустите приложение
```

### 5️⃣ Проверка (1 мин)
```bash
# Проверьте логи
tail -20 logs/vps_manager.log

# Откройте браузер
# http://localhost:5000/key-deployments
```

---

## ✅ Чеклист

- [ ] Создана резервная копия БД
- [ ] Скопированы все файлы
- [ ] Миграции применены
- [ ] Приложение перезагружено
- [ ] Логи проверены
- [ ] Страница /key-deployments открывается
- [ ] Таблица отображается

---

## 🔍 Что проверить

### Логирование работает?
```bash
grep INFO logs/vps_manager.log | tail -5
```

### Debug вывод удален?
```bash
grep "DEBUG" app.py
# Не должно быть результатов
```

### Индексы добавлены?
```bash
flask shell
>>> from app import db, KeyDeployment
>>> db.inspect(db.engine).get_indexes('key_deployments')
```

---

## 🆘 Если что-то не работает

### Проблема: "ENCRYPTION_KEY not set"
```bash
# Проверьте .env файл
cat .env | grep ENCRYPTION_KEY

# Если не установлен, сгенерируйте:
flask generate-fernet-key
```

### Проблема: "Миграция не применилась"
```bash
# Проверьте текущую версию
flask db current

# Примените вручную
flask db upgrade
```

### Проблема: "Таблица не отображается"
```bash
# Проверьте что есть данные
flask shell
>>> from app import db, KeyDeployment
>>> KeyDeployment.query.count()
```

### Проблема: "Логи не создаются"
```bash
# Создайте директорию
mkdir -p logs

# Проверьте права
chmod 755 logs
```

---

## 📊 Что изменилось

| Компонент | Было | Стало |
|-----------|------|-------|
| Безопасность | ❌ Утечки секретов | ✅ Защищено |
| Логирование | ❌ Отсутствует | ✅ Везде |
| Таймауты | ❌ Нет | ✅ Везде |
| Backup | ❌ Нет | ✅ Есть |
| Валидация | ❌ Нет | ✅ Есть |
| Индексы | ❌ Нет | ✅ Есть |
| Type hints | ❌ Нет | ✅ Везде |

---

## 📚 Дополнительно

- 📖 `README_AUDIT.md` - полный отчёт
- 🚀 `DEPLOYMENT_GUIDE.md` - подробная инструкция
- 📋 `LOGGING_GUIDE.md` - логирование
- 🔐 `SECURITY_AUDIT_SUMMARY.md` - анализ безопасности

---

## ✨ Готово!

Приложение обновлено и готово к использованию.

**Версия:** 2.0  
**Дата:** 31 октября 2025
