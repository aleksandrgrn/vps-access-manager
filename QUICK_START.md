# ⚡ Быстрый старт — VPS Manager

> **Время на установку:** ~10 минут  
> **Сложность:** Средняя  
> **Требуется:** Python 3.8+, Flask 3.0+, SQLAlchemy

---

## 🚀 Установка за 10 минут

### 1️⃣ Клонирование и настройка окружения (3 мин)

```bash
# Клонировать репозиторий
git clone <repository-url>
cd vps-access-manager

# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Активировать (Linux/macOS)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 2️⃣ Настройка переменных окружения (2 мин)

**Рекомендуемый способ — используйте встроенный скрипт:**

```bash
# Генерировать ключи и сохранить в .env
python generate_keys.py

# Выберите 'y' при запросе сохранения в файл
```

**Альтернативный способ — вручную:**

```bash
# Если вы хотите создать .env вручную
echo SECRET_KEY=$(python -c "import secrets; print(secrets.token_hex(32))") > .env
echo ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") >> .env
echo FLASK_ENV=development >> .env
echo DATABASE_URL=sqlite:///vps-access-manager.db >> .env
```

### 3️⃣ Инициализация базы данных (2 мин)

```bash
# Создать таблицы
python init_db.py

# Создать администратора (логин: admin, пароль: admin)
python seed_db.py

# Применить миграции (если есть)
flask db upgrade
```

### 4️⃣ Запуск приложения (1 мин)

```bash
# Режим разработки
python run.py

# Приложение доступно на http://localhost:5000
```

### 5️⃣ Первый вход (2 мин)

```
1. Открыть http://localhost:5000
2. Войти: admin / admin
3. ⚠️ ОБЯЗАТЕЛЬНО сменить пароль!
4. Добавить первый сервер
5. Сгенерировать SSH ключ
```

---

## ✅ Чеклист установки

- [ ] Python 3.8+ установлен
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Файл `.env` создан с ключами
- [ ] База данных инициализирована (`python init_db.py`)
- [ ] Администратор создан (`python seed_db.py`)
- [ ] Приложение запущено (`python run.py`)
- [ ] Вход выполнен и пароль изменен

---

## 🔍 Проверка установки

### Приложение запускается?
```bash
python run.py
# Должно быть: * Running on http://127.0.0.1:5000
```

### База данных создана?
```bash
ls -lh vps-access-manager.db
# Должен существовать файл БД
```

### Переменные окружения установлены?
```bash
cat .env | grep ENCRYPTION_KEY
# Должен показать ключ шифрования
```

### Модули импортируются?
```bash
python -c "from app import create_app; app = create_app(); print('✅ OK')"
# Должно вывести: ✅ OK
```

---

## 🆘 Решение проблем

### Проблема: "ModuleNotFoundError: No module named 'app'"
```bash
# Убедитесь что находитесь в корне проекта
cd vps-access-manager

# Проверьте структуру
ls -la app/
# Должны быть: __init__.py, models.py, routes/, services/
```

### Проблема: "ENCRYPTION_KEY not set"
```bash
# Сгенерируйте ключ шифрования
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# Добавьте в .env
echo "ENCRYPTION_KEY=<generated_key>" >> .env
```

### Проблема: "Address already in use"
```bash
# Порт 5000 занят, найдите процесс
lsof -i :5000  # Linux/macOS
netstat -ano | findstr :5000  # Windows

# Убейте процесс или используйте другой порт
python run.py --port 5001
```

### Проблема: "No such table: users"
```bash
# База данных не инициализирована
python init_db.py

# Если не помогло, удалите и пересоздайте
rm vps-access-manager.db
python init_db.py
python seed_db.py
```

### Проблема: "Cannot connect to server"
```bash
# Проверьте SSH доступ вручную
ssh -p 22 user@server_ip

# Проверьте что root ключ создан
python -c "from app import create_app, db; from app.models import SSHKey; app = create_app(); app.app_context().push(); print(SSHKey.query.filter_by(name='root').first())"
```

---

## 🏗️ Архитектура v2.0

### Новая структура проекта

```
vps-access-manager/
├── app/                    # Главный пакет
│   ├── __init__.py         # Application factory
│   ├── models.py           # SQLAlchemy модели
│   ├── forms.py            # WTForms
│   ├── utils.py            # Утилиты
│   ├── routes/             # Blueprints
│   │   ├── auth.py         # Аутентификация
│   │   ├── servers.py      # Серверы
│   │   ├── keys.py         # SSH ключи
│   │   └── deployments.py  # Развертывания
│   └── services/           # Бизнес-логика
│       └── key_service.py  # SSH операции
├── run.py                  # Точка входа ⭐
├── init_db.py              # Инициализация БД
└── seed_db.py              # Тестовые данные
```

### Ключевые изменения

| Компонент | Было (v1.0) | Стало (v2.0) |
|-----------|-------------|---------------|
| Точка входа | `app.py` (1626 строк) | `run.py` + модули |
| Архитектура | Монолит | Flask Blueprints |
| Маршруты | Все в `app.py` | `app/routes/*.py` |
| Бизнес-логика | В маршрутах | `app/services/*.py` |
| Запуск | `python app.py` | `python run.py` |
| Production | Сложно | `gunicorn run:app` |

---

## 🛠️ Служебные скрипты

### `generate_keys.py` — Генератор ключей

Генерирует безопасные ключи для production:

```bash
python generate_keys.py
```

Скрипт создаст:
- `SECRET_KEY` для Flask сессий и CSRF защиты
- `ENCRYPTION_KEY` для шифрования SSH-ключей

Выберите `y` для автоматического сохранения в `.env`.

### `init_db.py` — Инициализация БД

```bash
python init_db.py
```

Создает все таблицы базы данных.

### `seed_db.py` — Создание администратора

```bash
python seed_db.py
```

Создает пользователя `admin` с паролем `admin`.

**⚠️ Обязательно смените пароль после первого входа!**

---

## 🚀 Production развертывание

### С Gunicorn (рекомендуется)

```bash
# Установить Gunicorn
pip install gunicorn

# Запустить с 4 воркерами
gunicorn -w 4 -b 0.0.0.0:5000 "run:app"

# Или с systemd
sudo systemctl start vps-access-manager
```

### С Docker (опционально)

```bash
# Создать образ
docker build -t vps-access-manager .

# Запустить контейнер
docker run -d -p 5000:5000 \
  -v $(pwd)/vps-access-manager.db:/app/vps-access-manager.db \
  -v $(pwd)/.env:/app/.env \
  vps-access-manager
```

### С Nginx (reverse proxy)

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass_header Set-Cookie;
        proxy_set_header Cookie $http_cookie;
    }

    location /static {
        alias /var/www/vps-access-manager/app/static;
        expires 30d;
    }
}
```

---

## 📚 Дополнительные ресурсы

- 📖 **README.md** — полная документация проекта
- 🚀 **Production развертывание** — см. раздел выше

---

## ✨ Готово!

Приложение установлено и готово к использованию.

## ⚠️ Важно: HTTP без SSL

Если деплоите на HTTP (без SSL-сертификата), откройте `app/__init__.py` и измените:

```python
SESSION_COOKIE_SECURE=os.environ.get('FLASK_ENV') == 'production',
```

на:

```python
SESSION_COOKIE_SECURE=False,
```

Иначе сессии не будут работать.

---

**Версия:** 2.0  
**Дата:** Ноябрь 2025  
**Архитектура:** Flask Blueprints + Application Factory  
**Точка входа:** `run.py`
