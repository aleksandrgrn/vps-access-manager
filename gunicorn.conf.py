"""Gunicorn production config.

Подхватывается автоматически: gunicorn ищет ./gunicorn.conf.py в рабочем каталоге,
а юнит vps-access-manager.service запускается с WorkingDirectory на корне репозитория.
Править юнит не нужно — достаточно git pull и рестарта.

Здесь только то, чего нет в командной строке юнита: -w и -b она задаёт сама, и
аргументы командной строки перекрывают этот файл.
"""

# Дефолт gunicorn — 30 с, и его не хватает на добавление сервера: bootstrap ходит
# на машину три раза (версия sshd, запись ключа, проверка входа), а машины с
# маршрутизацией через цепочку VPS отвечают по 20+ с за заход. Воркер убивался
# посреди операции — в логе это видно как «VPS Manager startup» в середине запроса.
#
# Держать больше, чем vps_client.ADD_SERVER_TIMEOUT (90) на стороне pass-manager.
# Если поднять этот таймаут, а клиентский оставить коротким, будет хуже, чем сейчас:
# клиент положит трубку, а регистрация здесь дойдёт до конца, и машина останется
# без vps_manager_server_id в pass-manager — сирота, которую потом искать по IP.
timeout = 120
